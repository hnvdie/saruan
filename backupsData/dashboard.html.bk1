<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>Dashboard — {{ site_name }} Admin</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<style>
*{margin:0;padding:0;box-sizing:border-box}
:root{
  --bg:#fafaf8;--white:#fff;--border:#e8e4dc;--border2:#f0ece4;
  --ink:#1a1a1a;--muted:#888;--faint:#bbb;
  --gold:#c9a84c;--gold-bg:rgba(201,168,76,.07);
  --green:#22c55e;--green-bg:rgba(34,197,94,.08);
  --red:#ef4444;--red-bg:rgba(239,68,68,.08);
  --orange:#f97316;--orange-bg:rgba(249,115,22,.08);
  --blue:#3b82f6;--blue-bg:rgba(59,130,246,.08);
  --sans:'DM Sans',sans-serif;
}
body{background:var(--bg);color:var(--ink);font-family:var(--sans);font-weight:300;min-height:100vh;}
.layout{display:grid;grid-template-columns:210px 1fr;min-height:100vh;}

/* SIDEBAR */
.sidebar{background:var(--white);border-right:1px solid var(--border);position:sticky;top:0;height:100vh;overflow-y:auto;display:flex;flex-direction:column;}
.sidebar-logo{padding:1.2rem 1.5rem;border-bottom:1px solid var(--border);font-size:.95rem;font-weight:400;}
.sidebar-logo span{color:var(--gold);font-style:italic;}
.sidebar-logo small{display:block;font-size:.6rem;color:var(--muted);margin-top:.1rem;font-weight:300;letter-spacing:.05em;}
.nav-sec{padding:.7rem 1rem .2rem;font-size:.56rem;letter-spacing:.3em;text-transform:uppercase;color:var(--faint);}
.nav-item{display:flex;align-items:center;gap:.6rem;padding:.58rem 1.5rem;font-size:.76rem;color:var(--muted);text-decoration:none;transition:all .15s;border-left:2px solid transparent;}
.nav-item:hover{color:var(--ink);background:var(--bg);}
.nav-item.active{color:var(--ink);border-left-color:var(--gold);background:var(--gold-bg);font-weight:400;}
.sidebar-footer{margin-top:auto;padding:1rem 1.5rem;border-top:1px solid var(--border);}
.logout-link{display:flex;align-items:center;gap:.5rem;font-size:.72rem;color:var(--muted);text-decoration:none;transition:color .15s;}
.logout-link:hover{color:var(--red);}

/* MAIN */
.main{overflow:hidden;}
.topbar{padding:.85rem 1.75rem;background:var(--white);border-bottom:1px solid var(--border);display:flex;align-items:center;gap:1rem;position:sticky;top:0;z-index:50;}
.topbar-title{font-size:.9rem;font-weight:400;flex:1;}

/* SEARCH */
.search-form{display:flex;gap:.4rem;align-items:center;}
.search-wrap{position:relative;}
.search-icon{position:absolute;left:.6rem;top:50%;transform:translateY(-50%);font-size:.68rem;color:var(--faint);pointer-events:none;}
.search-input{padding:.48rem .75rem .48rem 2rem;border:1px solid var(--border);border-radius:2px;background:var(--bg);font-family:var(--sans);font-size:.76rem;color:var(--ink);outline:none;width:210px;transition:border-color .15s;}
.search-input:focus{border-color:var(--gold);}
.search-btn{padding:.48rem .85rem;border:1px solid var(--border);background:var(--white);border-radius:2px;font-family:var(--sans);font-size:.7rem;cursor:pointer;color:var(--muted);transition:all .15s;}
.search-btn:hover{border-color:var(--gold);color:var(--gold);}
.search-clear{font-size:.7rem;color:var(--muted);text-decoration:none;}
.search-clear:hover{color:var(--red);}

/* CONTENT */
.content{padding:1.4rem 1.75rem;}
.flash-msg{padding:.7rem 1rem;border-radius:2px;margin-bottom:1rem;font-size:.76rem;display:flex;align-items:center;gap:.55rem;}
.flash-msg.success{background:var(--green-bg);color:var(--green);border:1px solid rgba(34,197,94,.15);}
.flash-msg.warning{background:var(--orange-bg);color:var(--orange);border:1px solid rgba(249,115,22,.15);}
.flash-msg.info{background:var(--blue-bg);color:var(--blue);border:1px solid rgba(59,130,246,.15);}

/* STATS */
.stats-row{display:grid;grid-template-columns:repeat(4,1fr);gap:.75rem;margin-bottom:1.25rem;}
.stat-card{background:var(--white);border:1px solid var(--border);border-radius:3px;padding:1.1rem 1.25rem;}
.stat-num{font-size:1.7rem;font-weight:300;color:var(--ink);line-height:1;margin-bottom:.25rem;}
.stat-lbl{font-size:.6rem;letter-spacing:.1em;text-transform:uppercase;color:var(--muted);}

/* CARD */
.card{background:var(--white);border:1px solid var(--border);border-radius:3px;overflow:hidden;margin-bottom:1.25rem;}
.card-head{padding:.8rem 1.25rem;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;gap:.75rem;}
.card-title{font-size:.8rem;font-weight:400;}
.card-sub{font-size:.68rem;color:var(--muted);}

/* BUTTONS */
.btn{display:inline-flex;align-items:center;gap:.3rem;padding:.42rem .9rem;border:1px solid var(--border);border-radius:2px;font-family:var(--sans);font-size:.68rem;letter-spacing:.06em;text-transform:uppercase;cursor:pointer;color:var(--muted);background:transparent;text-decoration:none;transition:all .15s;}
.btn:hover{border-color:var(--gold);color:var(--gold);}
.btn.primary{background:var(--ink);color:#fff;border-color:var(--ink);}
.btn.primary:hover{background:#333;border-color:#333;color:#fff;}
.btn.danger:hover{border-color:var(--red);color:var(--red);}
.btn.success:hover{border-color:var(--green);color:var(--green);}

/* TABLE */
.table-wrap{overflow-x:auto;}
table{width:100%;border-collapse:collapse;}
th{padding:.6rem 1rem;border-bottom:1px solid var(--border);text-align:left;font-size:.6rem;letter-spacing:.12em;text-transform:uppercase;color:var(--muted);font-weight:400;white-space:nowrap;background:var(--bg);}
td{padding:.72rem 1rem;border-bottom:1px solid var(--border2);font-size:.76rem;vertical-align:middle;}
tr:last-child td{border-bottom:none;}
tr:hover td{background:var(--bg);}
.badge{display:inline-flex;align-items:center;gap:.2rem;padding:.15rem .45rem;border-radius:2px;font-size:.58rem;letter-spacing:.06em;text-transform:uppercase;}
.badge.active{background:var(--green-bg);color:var(--green);}
.badge.expired{background:var(--red-bg);color:var(--red);}
.badge.expiring{background:var(--orange-bg);color:var(--orange);}

/* THEME CARDS */
.themes-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(185px,1fr));gap:.65rem;padding:1.1rem;}
.tc{border:1px solid var(--border);border-radius:3px;overflow:hidden;}
.tc-prev{height:78px;display:flex;align-items:center;justify-content:center;position:relative;overflow:hidden;}
.tc-prev img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;opacity:.75;}
.tc-prev-lbl{position:relative;z-index:1;font-style:italic;font-size:.85rem;text-shadow:0 1px 4px rgba(0,0,0,.3);}
.tc-body{padding:.65rem .75rem;}
.tc-name{font-size:.78rem;font-weight:400;margin-bottom:.3rem;}
.tc-price{font-size:.7rem;color:var(--gold);margin-bottom:.3rem;min-height:1rem;}
.tc-btns{display:flex;gap:.3rem;}

/* THEME PHOTOS MANAGER — in-dashboard panel */
.theme-photos-panel{display:none;background:var(--bg);border-top:1px solid var(--border);padding:1rem 1.1rem;}
.theme-photos-panel.open{display:block;}
.tp-grid{display:flex;gap:.4rem;flex-wrap:wrap;margin-bottom:.75rem;}
.tp-photo{position:relative;width:72px;height:72px;border-radius:2px;overflow:hidden;border:1px solid var(--border);}
.tp-photo img{width:100%;height:100%;object-fit:cover;display:block;}
.tp-del{position:absolute;top:2px;right:2px;width:18px;height:18px;background:rgba(239,68,68,.85);color:#fff;border:none;border-radius:2px;cursor:pointer;font-size:.7rem;display:flex;align-items:center;justify-content:center;line-height:1;}
.tp-del:hover{background:var(--red);}
.tp-upload{display:flex;gap:.4rem;align-items:center;}
.tp-file-btn{padding:.42rem .85rem;border:1px dashed var(--border);border-radius:2px;font-family:var(--sans);font-size:.68rem;cursor:pointer;color:var(--muted);transition:all .15s;background:var(--white);}
.tp-file-btn:hover{border-color:var(--gold);color:var(--gold);}
.tp-hint{font-size:.65rem;color:var(--faint);}

/* EMPTY */
.empty{padding:2.5rem;text-align:center;color:var(--muted);}
.empty p{font-size:.8rem;margin-bottom:.3rem;}
.empty small{font-size:.7rem;color:var(--faint);}

/* MODAL */
.modal-overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,.4);z-index:200;align-items:center;justify-content:center;}
.modal-overlay.open{display:flex;}
.modal{background:var(--white);border-radius:3px;padding:1.65rem;width:100%;max-width:400px;border:1px solid var(--border);}
.modal h3{font-size:.88rem;font-weight:400;margin-bottom:.35rem;}
.modal p{font-size:.76rem;color:var(--muted);margin-bottom:1.4rem;line-height:1.7;}
.modal-actions{display:flex;gap:.5rem;justify-content:flex-end;}
.modal-input{width:100%;padding:.62rem .85rem;border:1px solid var(--border);border-radius:2px;font-family:var(--sans);font-size:.8rem;margin-bottom:.7rem;outline:none;color:var(--ink);}
.modal-input:focus{border-color:var(--gold);}
.modal-label{display:block;font-size:.62rem;letter-spacing:.12em;text-transform:uppercase;color:var(--muted);margin-bottom:.32rem;}

.search-result-tag{font-size:.7rem;color:var(--muted);margin-bottom:1rem;padding:.45rem .75rem;background:var(--gold-bg);border:1px solid rgba(201,168,76,.15);border-radius:2px;}

@media(max-width:768px){
  .layout{grid-template-columns:1fr;}
  .sidebar{display:none;}
  .stats-row{grid-template-columns:repeat(2,1fr);}
}
</style>
</head>
<body>
<div class="layout">

<!-- SIDEBAR -->
<aside class="sidebar">
  <div class="sidebar-logo">{{ site_name }}<span> Admin</span><small>v3 · Dashboard</small></div>
  <div class="nav-sec">Undangan</div>
  <a href="/admin" class="nav-item active">◈ Dashboard</a>
  <a href="/admin/create" class="nav-item">＋ Buat Undangan</a>
  <div class="nav-sec">Publik</div>
  <a href="/" target="_blank" class="nav-item">⌂ Landing Page</a>
  <a href="/themes" target="_blank" class="nav-item">◑ Katalog Tema</a>
  <div class="sidebar-footer">
    <a href="/admin/logout" class="logout-link">⎋ Logout</a>
  </div>
</aside>

<!-- MAIN -->
<div class="main">
  <div class="topbar">
    <div class="topbar-title">Dashboard</div>
    <form action="/admin" method="get" class="search-form">
      <div class="search-wrap">
        <span class="search-icon">⌕</span>
        <input class="search-input" name="q" type="text" placeholder="Cari nama / slug..." value="{{ search_q or '' }}">
      </div>
      <button type="submit" class="search-btn">Cari</button>
      {% if search_q %}<a href="/admin" class="search-clear">✕</a>{% endif %}
    </form>
    <a href="/admin/create" class="btn primary">＋ Buat</a>
  </div>

  <div class="content">

    {% with messages = get_flashed_messages(with_categories=true) %}
      {% for cat, msg in messages %}
      <div class="flash-msg {{ cat }}">{{ '✓' if cat=='success' else '⚠' }} {{ msg }}</div>
      {% endfor %}
    {% endwith %}

    {% if not search_q %}
    <div class="stats-row">
      <div class="stat-card"><div class="stat-num">{{ invitations|length }}</div><div class="stat-lbl">Undangan</div></div>
      <div class="stat-card"><div class="stat-num">{{ total_rsvp }}</div><div class="stat-lbl">Total RSVP</div></div>
      <div class="stat-card"><div class="stat-num">{{ themes|length }}</div><div class="stat-lbl">Tema</div></div>
      <div class="stat-card"><div class="stat-num">{{ invitations|selectattr('is_expired')|list|length }}</div><div class="stat-lbl">Expired</div></div>
    </div>
    {% endif %}

    {% if search_q %}
    <div class="search-result-tag">🔍 {{ invitations|length }} hasil untuk "<strong>{{ search_q }}</strong>"</div>
    {% endif %}

    <!-- UNDANGAN TABLE -->
    <div class="card">
      <div class="card-head">
        <div class="card-title">Daftar Undangan</div>
        <a href="/admin/create" class="btn primary">＋ Buat Baru</a>
      </div>
      {% if invitations %}
      <div class="table-wrap">
        <table>
          <thead><tr>
            <th>Pengantin</th><th>Link</th><th>Tema</th>
            <th>RSVP</th><th>Status</th><th>Aksi</th>
          </tr></thead>
          <tbody>
          {% for inv in invitations %}
          {% set days = inv.expires_at | days_left %}
          <tr>
            <td>
              <div style="font-weight:400">{{ inv.groom_name }} & {{ inv.bride_name }}</div>
              <div style="font-size:.66rem;color:var(--muted)">{{ inv.resepsi_date | format_date if inv.resepsi_date }}</div>
            </td>
            <td><a href="/i/{{ inv.slug }}" target="_blank" style="color:var(--blue);font-family:monospace;font-size:.7rem">/i/{{ inv.slug }}</a></td>
            <td style="font-size:.7rem;color:var(--muted)">{{ inv.theme_id }}</td>
            <td><span style="font-weight:400">{{ inv.rsvp_count }}</span> <span style="font-size:.63rem;color:var(--muted)">tamu</span></td>
            <td>
              {% if inv.is_expired %}<span class="badge expired">● Expired</span>
              {% elif days is not none and days <= 30 %}<span class="badge expiring">● {{ days }}hr</span>
              {% else %}<span class="badge active">● Aktif</span>{% endif %}
            </td>
            <td>
              <div style="display:flex;gap:.3rem;flex-wrap:wrap">
                <a href="/admin/edit/{{ inv.id }}" class="btn">Edit</a>
                <a href="/i/{{ inv.slug }}" target="_blank" class="btn">Lihat</a>
                {% if inv.is_expired %}
                <form method="post" action="/admin/extend/{{ inv.id }}" style="display:inline">
                  <button type="submit" class="btn success">+1Thn</button>
                </form>
                {% endif %}
                <button class="btn danger" onclick="confirmDel('{{ inv.id }}','{{ inv.groom_name }} & {{ inv.bride_name }}')">Hapus</button>
              </div>
            </td>
          </tr>
          {% endfor %}
          </tbody>
        </table>
      </div>
      {% else %}
      <div class="empty">
        <p>{% if search_q %}Tidak ada hasil{% else %}Belum ada undangan{% endif %}</p>
        {% if not search_q %}<small><a href="/admin/create" style="color:var(--gold)">Buat undangan pertama →</a></small>{% endif %}
      </div>
      {% endif %}
    </div>

    <!-- TEMA MANAGEMENT -->
    {% if not search_q %}
    <div class="card">
      <div class="card-head">
        <div>
          <div class="card-title">Manajemen Tema</div>
          <div class="card-sub">Atur harga &amp; foto katalog per tema</div>
        </div>
      </div>
      <div class="themes-grid">
        {% for theme in themes %}
        {% set fp = theme.get('demo_photos',[None])[0] %}
        <div class="tc" id="tc-{{ theme.id }}">
          <div class="tc-prev" style="background:{{ theme.preview_color }}">
            {% if fp %}<img src="{{ fp }}" alt="">{% endif %}
            <span class="tc-prev-lbl" style="color:{{ theme.accent_color }}">{{ theme.name }}</span>
          </div>
          <div class="tc-body">
            <div class="tc-name">{{ theme.name }}</div>
            <div class="tc-price" id="price-lbl-{{ theme.id }}">
              {% if theme.get('price_label') %}{{ theme.price_label }}
              {% elif theme.get('price') %}Rp {{ '{:,}'.format(theme.price).replace(',','.') }}
              {% else %}<span style="color:var(--faint);font-style:italic;font-size:.65rem">Belum ada harga</span>{% endif %}
            </div>
            <div class="tc-btns">
              <a href="/preview/{{ theme.id }}" target="_blank" class="btn" style="flex:1;justify-content:center">Demo</a>
              <button class="btn" style="flex:1;justify-content:center" onclick="openPhotos('{{ theme.id }}','{{ theme.name }}')">📷 Foto</button>
              <button class="btn" style="flex:1;justify-content:center" onclick="openPrice('{{ theme.id }}','{{ theme.name }}','{{ theme.get('price',0) }}','{{ theme.get('price_label','') }}','{{ theme.get('price_original',0) }}')">Harga</button>
            </div>
          </div>
          <!-- FOTO PANEL (inline, hidden by default) -->
          <div class="theme-photos-panel" id="photos-{{ theme.id }}">
            <div style="font-size:.65rem;letter-spacing:.1em;text-transform:uppercase;color:var(--muted);margin-bottom:.6rem">
              Foto Katalog — {{ theme.name }}
              <span style="float:right;cursor:pointer;color:var(--faint)" onclick="closePhotos('{{ theme.id }}')">✕ tutup</span>
            </div>
            <div class="tp-grid" id="tpgrid-{{ theme.id }}">
              {% for ph in theme.get('demo_photos',[]) %}
              <div class="tp-photo" id="tpph-{{ loop.index0 }}-{{ theme.id }}">
                <img src="{{ ph }}" alt="">
                <button class="tp-del" onclick="delThemePhoto('{{ theme.id }}','{{ ph }}',this.parentElement)" title="Hapus">✕</button>
              </div>
              {% endfor %}
            </div>
            <div class="tp-upload">
              <label class="tp-file-btn">
                ＋ Tambah Foto
                <input type="file" accept=".jpg,.jpeg,.png,.webp" multiple style="display:none" onchange="uploadThemePhotos('{{ theme.id }}',this)">
              </label>
              <span class="tp-hint">JPG/PNG/WebP, maks 5MB/foto. Foto pertama jadi cover katalog.</span>
            </div>
          </div>
        </div>
        {% endfor %}
      </div>
    </div>
    {% endif %}

  </div><!-- /content -->
</div><!-- /main -->
</div><!-- /layout -->

<!-- DELETE MODAL -->
<div class="modal-overlay" id="delModal">
  <div class="modal">
    <h3>Hapus Undangan?</h3>
    <p id="delModalMsg">Tindakan ini tidak bisa dibatalkan.</p>
    <div class="modal-actions">
      <button class="btn" onclick="closeModal('delModal')">Batal</button>
      <form id="delForm" method="post" style="display:inline">
        <button type="submit" class="btn danger" style="border-color:var(--red);color:var(--red)">Ya, Hapus</button>
      </form>
    </div>
  </div>
</div>

<!-- PRICE MODAL -->
<div class="modal-overlay" id="priceModal">
  <div class="modal">
    <h3 id="priceMTitle">Atur Harga</h3>
    <input type="hidden" id="priceThemeId">
    <label class="modal-label">Label Harga (tampil di katalog)</label>
    <input class="modal-input" id="priceLbl" type="text" placeholder="mis: Rp 299.000">
    <label class="modal-label">Nilai Angka (untuk sorting)</label>
    <input class="modal-input" id="priceVal" type="number" placeholder="299000">
    <label class="modal-label">Harga Coret (opsional)</label>
    <input class="modal-input" id="priceOrig" type="number" placeholder="399000">
    <div class="modal-actions">
      <button class="btn" onclick="closeModal('priceModal')">Batal</button>
      <button class="btn primary" onclick="savePrice()">Simpan</button>
    </div>
  </div>
</div>

<script>
// DELETE
function confirmDel(id, name){
  document.getElementById('delModalMsg').textContent = `Hapus undangan "${name}"? Tidak bisa dibatalkan.`;
  document.getElementById('delForm').action = `/admin/delete/${id}`;
  document.getElementById('delModal').classList.add('open');
}
function closeModal(id){ document.getElementById(id).classList.remove('open'); }
document.querySelectorAll('.modal-overlay').forEach(m=>m.addEventListener('click',e=>{if(e.target===m)m.classList.remove('open');}));

// PRICE MODAL
function openPrice(id, name, price, label, orig){
  document.getElementById('priceThemeId').value = id;
  document.getElementById('priceMTitle').textContent = `Harga: ${name}`;
  document.getElementById('priceLbl').value = label || '';
  document.getElementById('priceVal').value = price || '';
  document.getElementById('priceOrig').value = orig || '';
  document.getElementById('priceModal').classList.add('open');
}
async function savePrice(){
  const id = document.getElementById('priceThemeId').value;
  const body = {
    price: parseInt(document.getElementById('priceVal').value)||0,
    price_label: document.getElementById('priceLbl').value.trim(),
    price_original: parseInt(document.getElementById('priceOrig').value)||0,
  };
  const r = await fetch(`/admin/theme-price/${id}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});
  if(r.ok){
    closeModal('priceModal');
    const lbl = document.getElementById(`price-lbl-${id}`);
    if(lbl) lbl.textContent = body.price_label || (body.price ? 'Rp '+body.price.toLocaleString('id-ID') : '—');
  } else alert('Gagal menyimpan');
}

// THEME PHOTOS PANEL
let openPanel = null;
function openPhotos(id, name){
  if(openPanel && openPanel !== id){ closePhotos(openPanel); }
  const p = document.getElementById(`photos-${id}`);
  p.classList.toggle('open');
  openPanel = p.classList.contains('open') ? id : null;
}
function closePhotos(id){
  document.getElementById(`photos-${id}`)?.classList.remove('open');
  if(openPanel === id) openPanel = null;
}

// UPLOAD theme photos
async function uploadThemePhotos(themeId, input){
  if(!input.files.length) return;
  const form = new FormData();
  Array.from(input.files).forEach((f,i) => form.append(`photo${i}`, f));
  const r = await fetch(`/admin/theme-demo/${themeId}`, {method:'POST', body:form});
  const data = await r.json();
  if(data.success && data.urls){
    const grid = document.getElementById(`tpgrid-${themeId}`);
    data.urls.forEach(url => {
      const div = document.createElement('div');
      div.className = 'tp-photo';
      div.innerHTML = `<img src="${url}" alt=""><button class="tp-del" onclick="delThemePhoto('${themeId}','${url}',this.parentElement)">✕</button>`;
      grid.appendChild(div);
    });
    // Update card preview with first photo
    const prevImg = document.querySelector(`#tc-${themeId} .tc-prev img`);
    if(prevImg){ prevImg.src = data.urls[0]; }
    else {
      const prev = document.querySelector(`#tc-${themeId} .tc-prev`);
      const img = document.createElement('img');
      img.src = data.urls[0]; img.alt='';
      Object.assign(img.style,{position:'absolute',inset:'0',width:'100%',height:'100%',objectFit:'cover',opacity:'.75'});
      prev.insertBefore(img, prev.firstChild);
    }
  }
  input.value = '';
}

// DELETE theme photo
async function delThemePhoto(themeId, photoUrl, el){
  if(!confirm('Hapus foto ini?')) return;
  const r = await fetch(`/admin/theme-demo/${themeId}/delete`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({photo:photoUrl})});
  if((await r.json()).success){ el.remove(); }
}
</script>
</body>
</html>
